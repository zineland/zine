{% extends "base.jinja" %}

{% block content %}
    {% for season in seasons %}
        <div>
            <h2><a href="{{ season.slug }}">{{ season.title }}</a></h2>
            <p>{{ season.summary }}</p>
        </div>
    {% endfor %}
{% endblock content %}